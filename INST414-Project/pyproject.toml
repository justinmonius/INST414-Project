[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "player_value_prediction"
version = "0.0.1"
description = "A model predicting soccer player market value"
authors = [
  { name = "Justin Monius" },
]
license = { file = "LICENSE" }
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License"
]
requires-python = "~=3.10.0"


[tool.ruff]
line-length = 99
src = ["player_value_prediction"]
include = ["pyproject.toml", "player_value_prediction/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["player_value_prediction"]
force-sort-within-sections = true

